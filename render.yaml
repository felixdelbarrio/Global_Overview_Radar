services:
  - type: web
    name: global-overview-radar-api
    runtime: python
    plan: free
    buildCommand: pip install -r requirements.txt && pip install -e backend
    startCommand: uvicorn reputation.api.main:app --host 0.0.0.0 --port $PORT
    autoDeploy: true
