# Quality / tooling
ruff>=0.5.0
mypy>=1.10.0
pyright>=1.1.370

# Testing
pytest>=8.0.0
pytest-cov>=4.1.0
httpx>=0.27

# CodeQL (SAST)
# CodeQL CLI is not a Python package. Install it separately:
#   macOS: brew install codeql
# Then run: make codeql (or: make codeql-install)
